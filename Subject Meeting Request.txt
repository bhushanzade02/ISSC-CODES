#include<stdio.h>
int year ()
{
    int year;
    printf("Enter a year=");
    scanf("year=%d", &year);
   if ((year % 4 == 0 && year % 100 != 0) || (year % 400 == 0))
   {
    printf("%d is leap year", year);
    }
   else
   {printf("%d is not a leap year",year);}
   return 0;
   return 0;



}
